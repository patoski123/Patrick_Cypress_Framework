import { Given } from '@badeball/cypress-cucumber-preprocessor'
const chance = new Chance()

Given(/^A userType "([^"]*)" is created and added to the inspection as (.*)$/, function (userTypeValue, userRole, dataTable) {

})
